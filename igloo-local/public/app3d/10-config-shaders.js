const Be = {
    gridSize: 125,
    gridSizeLow: 50,
    gridSizeMobile: 25,
    topMargin: 90,
    topMarginLow: 45,
    topMarginMobile: 25,
    breakpointW: 1600,
    breakpointH: 800,
    breakPointMobile: 640,
    colorLogo: "#ffffff",
    colorTitle: "#3C3C54",
    colorText: "#ffffff",
    colorProjectTitle: "#67707E",
    colorProjectText: "#A1AAB7",
    manifesto: {
        title: "////// Hello",
        text: "Hi, I'm Yogi. Welcome to my corner of the internet."
    },
    copyright: "// ZEGIKA.COM / 2026",
    rights: `Built by YOGI
    Digital Experiences`,
    scroll: "Scroll down to discover.",
    follow: "/// Follow Us",
    click: "Klik Untuk Melihat",
    clickDisabled: "???????????????",
    close: "Close",
    social: [{
        name: "X",
        link: "https://twitter.com/iglooinc"
    }, {
        name: "LI",
        link: "https://www.linkedin.com/company/igloo-incorporated"
    }],
    cubes: [{
        title: "PORTFOLIO_YOGI Tentang Saya",
        hash: "pudgy-penguins",
        date: "28/08/2026",
        temp: 0,
        obj: "cube3",
        innerobject: "pudgy",
        interior: {
            enabled: !0,
            title: "////// Summary",
            content: `Saya Yogi Prasetya Sadewa, biasa dipanggil Yogi. Saat ini mahasiswa semester 7 Ilmu Komputer di Universitas Bina Bangsa Getsempena, Banda Aceh, IPK 3.70 — sambil ngerjain proyek web 3D sendiri secara freelance.

            Fokus saya: front-end web, WebGL, dan 3D web — bikin situs yang bukan cuma dibaca, tapi bisa kamu rasain. Stack andalan Three.js, React Three Fiber, GSAP, Next.js, sama Tailwind. 2024 jadi titik mulai serius, dan hampir tiap proyek sekarang selalu ada unsur 3D-nya.

            Cara kerja saya udah full AI-assisted. Claude Code jadi arsitek, OpenCode + OpenRouter ngerjain eksekusi, Obsidian nyimpen semua rancangan dan catatan. Hampir tiap hari ngobrol sama kombinasi AI — DeepSeek, GLM, Kimi, Claude — masing-masing buat hal yang beda. Kadang pakai Kiro atau VS Code buat prototyping cepat.

            Proyek yang lagi jalan: undangan.zegika.com (undangan pernikahan digital), lkps.zegika.com (sistem informasi), dan portowebGL — portfolio 3D yang sedang saya bangun. Buat lihat kode dan eksperimen lain, mampir ke github.com/axolotl-void.

            Di luar ngoding, saya senang desain dan main game. Target besarnya: jadi profesional di dunia AI dan 3D web developer — yang kombinatif, bukan salah satu doang.`,
            socialTitle: "/// Discover",
            social: [{
                name: "WA",
                link: "https://wa.me/6281260312799"
            }, {
                name: "IG",
                link: "https://www.instagram.com/gik_prasetya?igsh=MXg2NHkzYzBxaTZjYg%3D%3D"
            }, {
                name: "LI",
                link: "https://www.linkedin.com/in/yogi-prasetya-036b46323?utm_source=share_via&utm_content=profile&utm_medium=member_android"
            }, {
                name: "G",
                link: "mailto:yogiprasetya907@gmail.com"
            }],
            linkTitle: "/// Visit",
            links: [{
                name: "undangan",
                link: "https://undangan.zegika.com"
            }, {
                name: "zegika",
                link: "https://www.zegika.com"
            }],
            obj: "pudgy",
            objScale: 1.1
        }
    }, {
        title: "PORTFOLIO_YOGI_02 Skill & Proyek",
        hash: "skills-proyek",
        date: "28/08/2026",
        temp: -3,
        obj: "cube1",
        innerobject: "overpass_logo",
        interior: {
            enabled: !0,
            title: "////// Skill & Proyek",
            skills: [{
                name: "Three.js + React Three Fiber",
                icon: "threejs"
            }, {
                name: "Next.js + Tailwind",
                icon: "nextdotjs"
            }, {
                name: "GSAP + Framer Motion",
                icon: "gsap"
            }, {
                name: "Lenis",
                icon: null
            }, {
                name: "Howler.js",
                icon: null
            }, {
                name: "Claude Code",
                icon: "claude"
            }, {
                name: "OpenCode + OpenRouter",
                icon: null
            }, {
                name: "Obsidian",
                icon: "obsidian"
            }],
            technicalSkills: [{
                name: "▰▰▰▰▰▰▰▰▰▱  Custom GLSL shader (cuaca, kabut, kelambu)",
                icon: "threejs"
            }, {
                name: "▰▰▰▰▰▰▰▰▱▱  WebGL pipeline optimization",
                icon: "threejs"
            }, {
                name: "▰▰▰▰▰▰▰▱▱▱  Scroll-driven camera (spline CatmullRom)",
                icon: "gsap"
            }, {
                name: "▰▰▰▰▰▱▱▱▱▱  Procedural geometry & particle",
                icon: "threejs"
            }],
            content: `PROJECTS

▸ undangan.zegika.com    — undangan pernikahan digital Jepang
▸ lkps.zegika.com        — sistem informasi akademik
▸ portowebGL             — portfolio 3D (in progress)
▸ WebGL-Kage             — undangan Jepang procedural 3D

Kode & eksperimen lain ada di github.com/axolotl-void.`,
            socialTitle: "/// Discover",
            social: [],
            linkTitle: "/// Visit",
            links: [],
            obj: "overpass_logo",
            objScale: 1.2
        }
    }, {
        title: "PORTFOLIO_YOGI_03 Visi & Filosofi",
        hash: "visi-filosofi",
        date: "30/08/2026",
        temp: -5,
        obj: "cube2",
        innerobject: "abstractlogo",
        interior: {
            enabled: !0,
            title: "////// Visi & Filosofi",
            content: `KENAPA 3D WEB?

Saya percaya web yang cuma dibaca itu udah
lewat masanya. Web seharusnya bisa dirasain
— disentuh, dieksplorasi, dialami.

3D bukan hiasan. 3D adalah bahasa baru
untuk menyampaikan sesuatu yang kata-kata
dan gambar datar gak bisa capai.

Dari undangan nikah sampai portfolio ini —
setiap proyek saya adalah eksperimen:
bisakah web bikin orang bilang "wow"?


KENAPA AI-ASSISTED?

Saya bukan developer yang malu pakai AI.
Saya yang merangkul AI sebagai rekan kerja.

▸ Claude Code berpikir strategis
  — arsitektur, rencana, keputusan besar
▸ OpenCode + OpenRouter mengeksekusi
  — multi-model, cepat, parallel
▸ Obsidian menyimpan semuanya
  — rancangan, catatan, vault hidup

Hasilnya: satu orang bisa mengerjakan
apa yang biasanya butuh tim.

AI bukan pengganti skill.
AI adalah pengali dari skill yang sudah ada.


ARAH KE DEPAN

Target saya bukan jadi "developer biasa
yang bisa 3D." Tapi jadi orang yang ada
di persimpangan AI dan 3D web — tempat
yang belum banyak orang berdiri.

▸ Immersive web experiences
  yang accessible, bukan cuma demo tech
▸ AI-native workflow
  yang terbuka dan bisa direplikasi
▸ Proyek nyata yang berdampak
  bukan cuma eksperimen di sandbox

Kalau kamu sampai di kristal ini,
kamu udah lihat cara saya berpikir.

Mari bikin sesuatu bareng.`,
            socialTitle: "/// Connect",
            social: [{
                name: "WA",
                link: "https://wa.me/6281260312799"
            }, {
                name: "G",
                link: "mailto:yogiprasetya907@gmail.com"
            }],
            linkTitle: "/// Visit",
            links: [{
                name: "github",
                link: "https://github.com/axolotl-void"
            }, {
                name: "zegika",
                link: "https://www.zegika.com"
            }],
            obj: "abstractlogo",
            objScale: 1.2
        }
    }],
    links: [{
        title: "LinkedIn",
        url: "https://www.linkedin.com/company/igloo-incorporated",
        vdb: "peachesbody_64",
        scale: 1.2
    }, {
        title: "X / Twitter",
        url: "https://www.twitter.com/iglooinc",
        vdb: "x_64",
        scale: 1.3
    }, {
        title: "Medium",
        url: "https://medium.com/@iglooinc",
        vdb: "medium_32",
        scale: 1.25
    }, {
        title: "Axolotl 3D",
        url: "https://github.com/axolotl-void",
        vdb: "axolotl_64",
        scale: 1.3
    }],
    volume: 1,
    muted: !0
};
class u3 {
    constructor(e)
    {
        this.mainController = e,
        this._controller = new HU({
            volume: Be.volume,
            muted: Be.muted
        }),
        this.init()
    }
    init()
    {
        this._controller.addAudio({
            name: "music-bg",
            url: "music-highq.ogg",
            volume: .2,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "room-bg",
            url: "room.ogg",
            volume: .45,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "wind",
            url: "wind.ogg",
            volume: 0,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "igloo",
            url: "igloo.ogg",
            volume: 0,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "beeps",
            url: "beeps.ogg",
            volume: .5,
            minTimeBetweenPlays: .4
        }),
        this._controller.addAudio({
            name: "beeps2",
            url: "beeps2.ogg",
            volume: .5,
            minTimeBetweenPlays: .4
        }),
        this._controller.addAudio({
            name: "beeps3",
            url: "beeps3.ogg",
            volume: .5,
            minTimeBetweenPlays: .4
        }),
        this._controller.addAudio({
            name: "click-project",
            url: "click-project.ogg",
            volume: .5
        }),
        this._controller.addAudio({
            name: "enter-project",
            url: "enter-project.ogg",
            volume: .5
        }),
        this._controller.addAudio({
            name: "leave-project",
            url: "leave-project.ogg",
            volume: .5
        }),
        this._controller.addAudio({
            name: "shard",
            url: "shard.ogg",
            volume: 0,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "project-text",
            url: "project-text.ogg",
            volume: .5
        }),
        this._controller.addAudio({
            name: "portals",
            url: "circles.ogg",
            volume: 0,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "particles",
            url: "particles.ogg",
            volume: 0,
            autoPlay: !0,
            loop: !0
        }),
        this._controller.addAudio({
            name: "logo",
            url: "logo.ogg",
            volume: .3
        }),
        this._controller.addAudio({
            name: "ui-long",
            url: "ui-long.ogg",
            volume: .3
        }),
        this._controller.addAudio({
            name: "ui-short",
            url: "ui-short.ogg",
            volume: .3
        }),
        this._controller.addAudio({
            name: "manifesto",
            url: "manifesto.ogg",
            volume: .3
        }),
        Q.on("webgl_set_audio_volume", this.setAudioVolume, this),
        Q.on("webgl_play_audio", this.playAudio, this)
    }
    setAudioVolume(e, t=1)
    {
        this._controller.setAudioVolume(e, t)
    }
    playAudio(e)
    {
        this._controller.playAudio(e)
    }
}
var Ht = "float efit(float x,float a1,float a2,float b1,float b2){return b1+((x-a1)*(b2-b1))/(a2-a1);}float fit(float x,float a1,float a2,float b1,float b2){return clamp(efit(x,a1,a2,b1,b2),min(b1,b2),max(b1,b2));}float fit01(float x,float a1,float a2){return fit(x,0.0,1.0,a1,a2);}float fit10(float x,float a1,float a2){return fit(x,1.0,0.0,a1,a2);}float fit11(float x,float a1,float a2){return fit(x,-1.0,1.0,a1,a2);}vec3 efit(vec3 x,vec3 a1,vec3 a2,vec3 b1,vec3 b2){return b1+((x-a1)*(b2-b1))/(a2-a1);}vec3 fit(vec3 x,vec3 a1,vec3 a2,vec3 b1,vec3 b2){return clamp(efit(x,a1,a2,b1,b2),min(b1,b2),max(b1,b2));}vec3 fit01(vec3 x,vec3 a1,vec3 a2){return fit(x,vec3(0.0),vec3(1.0),a1,a2);}vec3 fit10(vec3 x,vec3 a1,vec3 a2){return fit(x,vec3(1.0),vec3(0.0),a1,a2);}vec3 fit11(vec3 x,vec3 a1,vec3 a2){return fit(x,vec3(-1.0),vec3(1.0),a1,a2);}",
    d3 = `float ca_linterp(float t){return clamp(1.0-abs(2.0*t-1.0),0.0,1.0);}float ca_remap(float t,float a,float b){return clamp((t-a)/(b-a),0.0,1.0);}vec2 ca_barrelDistortion(vec2 coord,float amt){vec2 cc=coord-0.5;float dist=dot(cc,cc);return coord+cc*dist*amt;}vec4 ca_spectrum_offset(float t){vec4 ret;float lo=step(t,0.5);float hi=1.0-lo;float w=ca_linterp(ca_remap(t,1.0/6.0,5.0/6.0));ret=vec4(lo,1.0,hi,1.0)*vec4(1.0-w,w,1.0-w,1.0);return pow(ret,vec4(1.0/2.2));}
    #ifndef CA_ITERATIONS
    const int CA_ITERATIONS=5;
    #endif
    const float RECI_ITER=1.0/float(CA_ITERATIONS);vec4 chromatic_aberration(sampler2D text,vec2 uv,float maxdistort,float bendAmount){vec4 sumcol=vec4(0.0);vec4 sumw=vec4(0.0);for(int i=0;i<CA_ITERATIONS;++i){float t=float(i)*RECI_ITER;vec4 w=ca_spectrum_offset(t);sumw+=w;sumcol+=w*texture2D(text,ca_barrelDistortion(uv,bendAmount*maxdistort*t));}return sumcol/sumw;}`,
    Rc = `#ifndef PI
    #define PI 3.141592653589793
    #endif
    #ifndef HALF_PI
    #define HALF_PI 1.5707963267948966
    #endif
    float backIn(float t){return pow(t,3.)-t*sin(t*PI);}float backOut(float t){return 1.0-backIn(1.0-t);}float backInOut(float t){float f=t<0.5? 2.0*t: 1.0-(2.0*t-1.0);float g=backIn(f);return t<0.5? 0.5*g: 0.5*(1.0-g)+0.5;}float bounceOut(float t){const float a=4.0/11.0;const float b=8.0/11.0;const float c=9.0/10.0;const float ca=4356.0/361.0;const float cb=35442.0/1805.0;const float cc=16061.0/1805.0;float t2=t*t;return t<a? 7.5625*t2: t<b? 9.075*t2-9.9*t+3.4: t<c? ca*t2-cb*t+cc: 10.8*t*t-20.52*t+10.72;}float bounceIn(float t){return 1.0-bounceOut(1.0-t);}float bounceInOut(float t){return t<0.5? 0.5*(1.0-bounceOut(1.0-t*2.0)): 0.5*bounceOut(t*2.0-1.0)+0.5;}float circularIn(float t){return 1.0-sqrt(1.0-t*t);}float circularOut(float t){return sqrt((2.0-t)*t);}float circularInOut(float t){return t<0.5? 0.5*(1.0-sqrt(1.0-4.0*t*t)): 0.5*(sqrt((3.0-2.0*t)*(2.0*t-1.0))+1.0);}float cubicIn(float t){return t*t*t;}float cubicOut(float t){float f=t-1.0;return f*f*f+1.0;}float cubicInOut(float t){return t<0.5? 4.0*t*t*t: 0.5*-pow(2.0-2.0*t,3.0)+1.0;}float elasticIn(float t){return sin(13.0*t*HALF_PI)*pow(2.0,10.0*(t-1.0));}float elasticOut(float t){return sin(-13.0*(t+1.0)*HALF_PI)*pow(2.0,-10.0*t)+1.0;}float elasticInOut(float t){return t<0.5? 0.5*sin(+13.0*HALF_PI*2.0*t)*pow(2.0,10.0*(2.0*t-1.0)): 0.5*sin(-13.0*HALF_PI*((2.0*t-1.0)+1.0))*pow(2.0,-10.0*(2.0*t-1.0))+1.0;}float expoIn(float t){return t==0.0 ? t : pow(2.0,10.0*(t-1.0));}float expoOut(float t){return t==1.0 ? t : 1.0-pow(2.0,-10.0*t);}float expoInOut(float t){return t==0.0||t==1.0? t: t<0.5?+0.5*pow(2.0,(20.0*t)-10.0):-0.5*pow(2.0,10.0-(t*20.0))+1.0;}float linear(float t){return t;}float quadraticIn(float t){return t*t;}float quadraticOut(float t){return-t*(t-2.0);}float quadraticInOut(float t){float p=2.0*t*t;return t<0.5 ? p :-p+(4.0*t)-1.0;}float quarticIn(float t){return pow(t,4.0);}float quarticOut(float t){return pow(1.0-t,3.0)*(t-1.0)+1.0;}float quarticInOut(float t){return t<0.5?+8.0*pow(t,4.0):-8.0*pow(1.0-t,4.0)+1.0;}float quinticIn(float t){return pow(t,5.0);}float quinticOut(float t){return 1.0-pow(1.0-t,5.0);}float quinticInOut(float t){return t<0.5?+16.0*pow(t,5.0):-0.5*pow(2.0-2.0*t,5.0)+1.0;}float sineIn(float t){return sin((t-1.0)*HALF_PI)+1.0;}float sineOut(float t){return sin(t*HALF_PI);}float sineInOut(float t){return-0.5*(cos(PI*t)-1.0);}
    #define power0(t) linear(t)
    #define power1In(t) quadraticIn(t)
    #define power1Out(t) quadraticOut(t)
    #define power1InOut(t) quadraticInOut(t)
    #define power2In(t) cubicIn(t)
    #define power2Out(t) cubicOut(t)
    #define power2InOut(t) cubicInOut(t)
    #define power3In(t) quarticIn(t)
    #define power3Out(t) quarticOut(t)
    #define power3InOut(t) quarticInOut(t)
    #define power4In(t) quinticIn(t)
    #define power4Out(t) quinticOut(t)
    #define power4InOut(t) quinticInOut(t)
    `,
    Uc = "vec4 getNoise(sampler2D tex,vec2 uv,vec2 offset){float invSize=1.0/float(textureSize(tex,0).x);return texture(tex,uv*invSize+offset);}";
class f3 extends fe {
    constructor()
    {
        super({
            uniformsGroups: [he.UBO],
            uniforms: {
                tScene1: {
                    value: null
                },
                tScene2: {
                    value: null
                },
                tScroll: {
                    value: le.load("scroll-datatexture.ktx2", "colordata-repeat")
                },
                uProgress: {
                    value: 0
                },
                uProgressVel: {
                    value: 0
                },
                tDetail: {
                    value: null
                },
                uInCubes: {
                    value: !1
                },
                tCubes: {
                    value: null
                },
                uDetailProgress: {
                    value: 0
                },
                uDetailProgress2: {
                    value: 0
                },
                tFrost: {
                    value: le.load("frost-datatexture.ktx2", "colordata-repeat")
                },
                tBlue: {
                    value: le.load("noises/blue-8-128-rgb.ktx2", "colordata-repeat")
                },
                uBlueOffset: {
                    value: new H
                }
            },
            vertexShader: `
                            //- edit
                            varying vec2 vUv;

                            void main() {
                                vUv = uv;
                                gl_Position = vec4(position, 1.0);
                            }
                        `,
            fragmentShader: `
                            //- edit
                            ${ae}
                            ${Ht}
                            ${Ue}
                            ${Rc}
                            ${Uc}

                            #define CA_ITERATIONS 5
                            ${d3}

                            uniform sampler2D tScene1;
                            uniform sampler2D tScene2;
                            uniform sampler2D tScroll;
                            uniform sampler2D tBlue;
                            uniform vec2 uBlueOffset;
                            uniform float uProgress;
                            uniform float uProgressVel;

                            uniform sampler2D tDetail;
                            uniform sampler2D tCubes;
                            uniform sampler2D tFrost;
                            uniform bool uInCubes;
                            uniform float uDetailProgress;
                            uniform float uDetailProgress2;

                            varying vec2 vUv;

                            void main() {

                                vec3 color = vec3(0.0);

                                /*
                                // horizontal cut effect that reacts to velocity
                                float slope = 0.15 * uProgressVel * aspect * step(0.0, uProgress);
                                float inclination = mix(1.0 - vUv.x, vUv.x, step(slope, 0.0));
                                float cut = aastep(vUv.y + inclination * abs(slope), fit(uProgress, 0.0, 1.0, 0.0, 1.0 + abs(slope)));
                                */

                                if (uProgress > 0.0) {
                                    // cut
                                    vec2 uvTex = vUv - 0.5;
                                    uvTex.x *= aspect;
                                    uvTex += 0.5;
                                    vec3 scrollTex = texture2D(tScroll, uvTex * 1.0).rgb;

                                    // slope displacement
                                    float slopeDisp = (scrollTex.b * 2.0 - 1.0) * 0.4;
                                    float slope = -0.2 * aspect * step(0.0, uProgress);
                                    float inclination = mix(1.0 - vUv.x + slopeDisp, vUv.x + slopeDisp, step(slope, 0.0));
                                    float incProgress = fit(uProgress, 0.0, 1.0, 0.0, 1.0 + abs(slope));

                                    // cromatic aberration
                                    float cutDiagonalBlur = falloff(vUv.y + inclination * abs(slope), 0.0, 1.0, 2.0, incProgress);

                                    // tech displacement
                                    float cutDiagonalDisplacement = falloff(vUv.y + inclination * abs(slope), 0.0, 1.0, 0.9, incProgress);
                                    float cutDisp = falloff(scrollTex.g, 0.0, 1.0, 1.0, cutDiagonalDisplacement);

                                    // ice cut
                                    float cutDiagonal = falloff(vUv.y + inclination * abs(slope), 0.0, 1.0, 0.2, incProgress);
                                    float cut = falloff(scrollTex.r, 0.0, 1.0, 2.0, cutDiagonal);

                                    // vars
                                    const float parallaxY = 0.4;
                                    const float displacement = 0.025;

                                    // add blue noise to hide the seams from the chromatic aberration
                                    vec4 noise = getNoise(tBlue, gl_FragCoord.xy, uBlueOffset);

                                    // mix with optimization to save texture reads
                                    vec3 scene1 = vec3(0.0);
                                    vec3 scene2 = vec3(0.0);
                                    float modulator = 12.0 * smoothstep(1.0, 0.7, abs(vUv.x * 2.0 - 1.0)) * smoothstep(1.0, 0.7, abs(vUv.y * 2.0 - 1.0));
                                    if (cut < 1.0) scene1 = chromatic_aberration(tScene1, vUv - vec2(0.0, parallaxY * power2In(uProgress) + displacement * cutDisp), modulator, cutDiagonalBlur * noise.r).rgb;
                                    if (cut > 0.0) scene2 = chromatic_aberration(tScene2, vUv + vec2(0.0, parallaxY * power2In(1.0 - uProgress) + displacement * (1.0 - cutDisp)), modulator, (1.0 - cutDiagonalBlur) * noise.g).rgb;
                                    color = clamp(mix(scene1, scene2, cut), vec3(0.0), vec3(1.0));

                                } else {
                                    // detail overlay
                                    if (uInCubes && uDetailProgress > 0.0) {
                                        if (uDetailProgress < 1.0 || uDetailProgress2 < 1.0) {
                                            // normalized uv
                                            vec2 uvTex = vUv - 0.5;
                                            uvTex.x *= aspect;
                                            uvTex += 0.5;

                                            // transition
                                            float transition = fit(uDetailProgress, 0.4, 1.0, 0.0, 1.0);

                                            // blue noise to hide the seams from the chromatic aberration
                                            vec4 noise = getNoise(tBlue, gl_FragCoord.xy, uBlueOffset);
                                            float modulator = 8.0 * smoothstep(1.0, 0.5, abs(vUv.x * 2.0 - 1.0)) * smoothstep(1.0, 0.5, abs(vUv.y * 2.0 - 1.0));

                                            // tech displacement
                                            vec2 techDisp = uvTex - 0.5;
                                            techDisp *= 0.1;
                                            float tTech = texture(tScroll, techDisp).g * 2.0 - 1.0;
                                            float dispTechStr = 0.005;
                                            vec2 dispTech = vec2(1.0, 0.0) * tTech * dispTechStr;

                                            // ice displacement
                                            vec2 uvDisp = uvTex - 0.5;
                                            uvDisp *= 5.0;
                                            uvDisp *= (1.0 - uDetailProgress);
                                            float tDisp = texture(tFrost, uvDisp).r * 2.0 - 1.0;
                                            float dispStr = 0.1;
                                            vec2 disp = vec2(1.0, 0.0) * tDisp * dispStr;
                                            disp += vec2(0.0, 1.0) * tDisp * dispStr;

                                            vec3 scene;
                                            if (transition < 1.0) {
                                                vec2 sceneIceDisp = disp * power1In(fit(uDetailProgress, 0.1, 0.9, 0.0, 1.0));
                                                vec2 sceneTechDisp = dispTech * power4Out(uDetailProgress);
                                                scene = chromatic_aberration(tCubes, vUv + sceneIceDisp + sceneTechDisp, modulator, power2Out(uDetailProgress) * noise.r).rgb;
                                            }

                                            vec3 detail;
                                            if (transition > 0.0) {
                                                vec2 detailIceDisp = disp * fit(uDetailProgress, 0.1, 0.9, 1.0, 0.0);
                                                vec2 detailTechDisp = dispTech * fit(uDetailProgress2, 0.7, 1.0, 1.0, 0.0);
                                                detail = chromatic_aberration(tDetail, vUv + detailIceDisp + detailTechDisp, modulator, power2Out(1.0 - uDetailProgress2) * noise.r).rgb;
                                            }

                                            // mix both scenes
                                            color = mix(scene, detail, transition);
                                        } else {
                                            color = texture2D(tDetail, vUv).rgb;
                                        }
                                    } else {
                                        color = texture2D(tScene1, vUv).rgb;
                                    }
                                }

                                gl_FragColor = vec4(color, 1.0);
                            }
                        `,
            depthTest: !1,
            depthWrite: !1
        }),
        Q.on("webgl_render", () => {
            this.uniforms.uBlueOffset.value.set(Math.random() * 10, Math.random() * 12.5)
        })
    }
}
class p3 extends fe {
    constructor()
    {
        super({
            uniformsGroups: [he.UBO],
            uniforms: {
                tScene: {
                    value: null
                },
                uColor: {
                    value: new Z("#8b909d")
                },
                uIntro: {
                    value: 0
                }
            },
            vertexShader: `
                            //- edit
                            varying vec2 vUv;

                            void main() {
                                vUv = uv;
                                gl_Position = vec4(position, 1.0);
                            }
                        `,
            fragmentShader: `
                            //- edit
                            ${ae}

                            uniform sampler2D tScene;
                            uniform float uIntro;
                            uniform vec3 uColor;

                            varying vec2 vUv;

                            void main() {
                                vec4 scene = texture2D(tScene, vUv);
                                vec3 color = scene.rgb;

                                // final overlay
                                color = mix(uColor, color, uIntro);

                                // color
                                gl_FragColor = vec4(color, 1.0);
                            }
                        `,
            depthTest: !1,
            depthWrite: !1
        })
    }
}
var xE = `#ifdef GL_FRAGMENT_PRECISION_HIGH
precision highp sampler3D;
#else
precision mediump sampler3D;
#endif
vec3 LUTLinearTosRGB(in vec3 value){return mix(pow(value.rgb,vec3(0.41666))*1.055-vec3(0.055),value.rgb*12.92,vec3(lessThanEqual(value.rgb,vec3(0.0031308))));}vec3 LUTsRGBToLinear(in vec3 value){return mix(pow(value.rgb*0.9478672986+vec3(0.0521327014),vec3(2.4)),value.rgb*0.0773993808,vec3(lessThanEqual(value.rgb,vec3(0.04045))));}vec3 apply3DLUT(vec3 color,sampler3D lutTexture,float lutSize,float lutIntensity){float scale=(lutSize-1.0)/lutSize;float offset=1.0/(2.0*lutSize);vec3 col=LUTLinearTosRGB(color);vec3 rgb=col*scale+offset;vec4 lutColor=texture(lutTexture,rgb);return LUTsRGBToLinear(mix(col,lutColor.rgb,lutIntensity));}vec3 apply3DLUTTetrahedral(vec3 color,sampler3D lutTexture,float lutSize,float lutIntensity){float scale=lutSize-1.0;float offset=0.0;float texelSize=1.0/lutSize;vec3 col=LUTLinearTosRGB(color);vec3 rgb=clamp(col,0.0,1.0)*scale+offset;vec3 p=floor(rgb);vec3 f=rgb-p;vec3 v1=(p+0.5)*texelSize;vec3 v4=(p+1.5)*texelSize;vec3 v2,v3;vec3 frac;if(f.r>=f.g){if(f.g>f.b){frac=f.rgb;v2=vec3(v4.x,v1.y,v1.z);v3=vec3(v4.x,v4.y,v1.z);}else if(f.r>=f.b){frac=f.rbg;v2=vec3(v4.x,v1.y,v1.z);v3=vec3(v4.x,v1.y,v4.z);}else{frac=f.brg;v2=vec3(v1.x,v1.y,v4.z);v3=vec3(v4.x,v1.y,v4.z);}}else{if(f.b>f.g){frac=f.bgr;v2=vec3(v1.x,v1.y,v4.z);v3=vec3(v1.x,v4.y,v4.z);}else if(f.r>=f.b){frac=f.grb;v2=vec3(v1.x,v4.y,v1.z);v3=vec3(v4.x,v4.y,v1.z);}else{frac=f.gbr;v2=vec3(v1.x,v4.y,v1.z);v3=vec3(v1.x,v4.y,v4.z);}}vec4 n1=texture(lutTexture,v1);vec4 n2=texture(lutTexture,v2);vec4 n3=texture(lutTexture,v3);vec4 n4=texture(lutTexture,v4);vec4 weights=vec4(1.0-frac.x,frac.x-frac.y,frac.y-frac.z,frac.z);vec4 result=weights*mat4(vec4(n1.r,n2.r,n3.r,n4.r),vec4(n1.g,n2.g,n3.g,n4.g),vec4(n1.b,n2.b,n3.b,n4.b),vec4(1.0));return LUTsRGBToLinear(mix(col,result.rgb,lutIntensity));}`;
class Eg extends vE {
    constructor(e, t)
    {
        super(),
        this.textureID = t !== void 0 ? t : "tDiffuse",
        e instanceof fe ? (this.uniforms = e.uniforms, this.material = e) : e && (this.uniforms = UA.clone(e.uniforms), this.material = new fe({
            name: e.name !== void 0 ? e.name : "unspecified",
            defines: Object.assign({}, e.defines),
            uniforms: this.uniforms,
            vertexShader: e.vertexShader,
            fragmentShader: e.fragmentShader
        })),
        this.fsQuad = new wg(this.material)
    }
    render(e, t, s)
    {
        this.uniforms[this.textureID] && (this.uniforms[this.textureID].value = s.texture),
        this.fsQuad.material = this.material,
        this.renderToScreen ? (e.setRenderTarget(null), this.fsQuad.render(e)) : (e.setRenderTarget(t), this.clear && e.clear(e.autoClearColor, e.autoClearDepth, e.autoClearStencil), this.fsQuad.render(e))
    }
    dispose()
    {
        this.material.dispose(),
        this.fsQuad.dispose()
    }
}
let m3 = class  extends fe{
    constructor()
    {
        super({
            uniformsGroups: [he.UBO],
            uniforms: {
                tDiffuse: {
                    value: null
                },
                tLUT: {
                    value: le.load("igloo/igloo_scene.ktx2", "luttetrahedral")
                },
                uLUTSize: {
                    value: 1
                },
                uLUTIntensity: {
                    value: 1
                },
                uGradientAlpha: {
                    value: 0
                }
            },
            vertexShader: `
                            ${ae}

                            varying vec2 vUv;

                            void main() {
                                vUv = uv;
                                gl_Position = vec4(position, 1.0);
                            }
                        `,
            fragmentShader: `
                            ${ae}

                            uniform sampler2D tDiffuse;

                            varying vec2 vUv;

                            ${xE}

                            uniform sampler3D tLUT;
                            uniform float uLUTSize;
                            uniform float uLUTIntensity;
                            uniform float uGradientAlpha;

                            vec3 hash32(vec2 p) {
                                vec3 p3 = fract(vec3(p.xyx) * vec3(.1031, .1030, .0973));
                                p3 += dot(p3, p3.yxz+33.33);
                                return fract((p3.xxy+p3.yzz)*p3.zyx);
                            }

                            void main() {
                                vec2 uv = vUv;

                                // apply rgb shift
                                // float offset = 0.08 * length(vUv - 0.5);
                                // offset = pow(offset, 2.0);
                                // float r = texture2D(tDiffuse, uv + vec2(-offset, offset)).r;
                                // float g = texture2D(tDiffuse, uv).g;
                                // float b = texture2D(tDiffuse, uv + vec2(offset, -offset)).b;
                                // vec3 scene = vec3(r, g, b);

                                vec3 scene = texture2D(tDiffuse, uv).rgb;
                                float gradient = mix(0.8, 1.0, (uv.x + uv.y) * 0.5);
                                gradient = mix(1.0, gradient, uGradientAlpha);
                                scene *= gradient;

                                vec3 sceneColor = apply3DLUTTetrahedral(scene.rgb, tLUT, uLUTSize, uLUTIntensity);

                                // film grain
                                // vec3 noise = hash32(uv * 1000.0 + time);
                                // noise *= 2.0;
                                // noise -= 1.0;
                                // sceneColor += noise * 0.05;

                                gl_FragColor = vec4(sceneColor, 1.0);
                            }
                        `
        }),
        setTimeout(async () => {
            const e = this.uniforms.tLUT.value;
            await e._loaded,
            this.uniforms.uLUTSize.value = e.image.width
        }, 0)
    }
}
;
async function A3(i, e) {
    const t = new Eg(new m3);
    t.isIglooColorCorrectionPass = !0,
    e.addPass(t),
    await t.material.uniforms.tLUT.value._loaded
}
