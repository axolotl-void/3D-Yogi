{
  "name": "igloo-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble-app3d.mjs",
    "dev": "vite",
    "predev": "npm run assemble",
    "build": "vite build",
    "prebuild": "npm run assemble",
    "verify:app3d": "node scripts/assemble-app3d.mjs --verify",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.0"
  }
}
